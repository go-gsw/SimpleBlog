const getters ={
	getDone(){
		return states.event.filter(function(d){
			return
		})
	},
	ischangeTools(state){
		// let state.isShow=!state.isShow
		return this.state.isShow
		// return false
	}
}
export default {
	getters
}